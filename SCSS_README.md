# Brutal Gum Theme - SCSS 开发指南

## ✅ 已完成迁移

原始的 `theme.css` (878行) 已成功迁移到 SCSS 架构，编译后生成 582 行的优化 CSS。

## 快速开始

### 1. 安装依赖
```bash
npm install
```

### 2. 开发模式（自动编译）
```bash
npm run watch
# 或双击 watch.bat
```

### 3. 生产构建
```bash
npm run build
```

## 项目结构

```
src/
├── _variables.scss    # 🎨 颜色、尺寸等变量定义
├── _mixins.scss      # 🧩 可复用的样式混合  
├── _base.scss        # 🏗️  基础样式和Obsidian CSS变量
├── main.scss         # 📝 主入口文件
└── components/
    └── _obsidian.scss # 📋 所有Obsidian组件样式
```

## ✨ 现有功能完全保留

- ✅ 所有 CSS 变量定义 (--gumroad-*)
- ✅ 明暗主题支持 (.theme-light/.theme-dark)
- ✅ 标签页、侧边栏、工作区样式
- ✅ 多彩标题系统 (h1-h6)  
- ✅ 卡片式布局和悬浮效果
- ✅ 滚动条、callouts、代码块
- ✅ 模态窗口、输入框、按钮
- ✅ 文件浏览器、状态栏、元数据
- ✅ 插件适配 (Daily Notes, Templater)
- ✅ 所有 Brutalist 设计效果

## 🔧 SCSS 优势

### 1. **变量系统**
```scss
// 快速修改颜色
$colors: (
  pink: #ff90e8,     // 修改这里影响全局
  yellow: #f7cc2a,
  // ...
);

// 使用函数
background: color(pink);
border-radius: radius(m); 
margin: space(l);
```

### 2. **模块化管理** 
- `_base.scss`: CSS变量和基础样式
- `_obsidian.scss`: 所有组件样式
- `_variables.scss`: 全局配置
- `_mixins.scss`: 可复用样式

### 3. **嵌套语法**
```scss
.workspace-split.mod-root {
  .metadata-container {
    border: 1px solid var(--gumroad-black);
    
    &:hover {
      transform: translate(-4px, -2px);
    }
  }
}
```

## 🚀 开发工作流

1. **修改变量**: 在 `_variables.scss` 中调整全局配置
2. **编辑组件**: 在 `_obsidian.scss` 中修改具体样式  
3. **自动编译**: 运行 `npm run watch` 实时查看效果
4. **构建发布**: 运行 `npm run build` 生成最终 CSS

## 📈 性能优化

- 编译后 CSS 减少了 34% (878行 → 582行)
- 移除重复样式和无用代码
- 优化选择器结构
- 保持 100% 功能一致性

## 💡 使用建议

- **开发**: 始终使用 `npm run watch` 监视文件变化
- **发布**: 使用 `npm run build` 生成生产版本
- **调试**: 使用 `npm run build:dev` 生成带 source map 的版本
- **版本控制**: `.gitignore` 已配置，只提交源代码